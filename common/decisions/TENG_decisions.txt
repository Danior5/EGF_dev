foreign_politics = {		
	pregrided_generals= {
		icon = GFX_decision_icon_default

		available = {
			has_army_experience > 74
		}
		custom_cost_trigger = {
			has_army_experience > 74
		}
		
		custom_cost_text = decision_cost_army_xp_75
		
		fire_only_once = yes

		visible = {		
			is_ai = no
		}
		
		complete_effect = {
			custom_effect_tooltip = "choose your own general!"
			army_experience = -75
			hidden_effect = {
				if = { 
					limit = { TAG = bak }
					country_event = {
						id = teng_ww.1
					}
				}
				else_if = {
					limit = { TAG = bat }
					country_event = {
						id = teng_ww.2
					}
				}
				else_if = {
					limit = { TAG = chn }
					country_event = {
						id = teng_ww.3
					}
				}
				else_if = {
					limit = { TAG = cry }
					country_event = {
						id = teng_ww.4
					}
				}
				else_if = {
					limit = { TAG = eqs }
					country_event = {
						id = teng_ww.5
					}
				}
				else = {
					country_event = {
						id = teng_ww.99
					}
				}
			}
		}
		ai_will_do = {	#Modifiers for the AI to decide when to execute the decision.
			factor = 0			
		}
	}
}